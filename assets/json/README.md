# `./assets/json`

This directory stores the JSON data for use with [keyboard-layout-editor.com](https://www.keyboard-layout-editor.com/).

Drag and drop `.json` files directly into the “Raw data” tab on the site.
